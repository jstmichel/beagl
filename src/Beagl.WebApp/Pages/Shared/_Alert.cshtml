@model Beagl.WebApp.Pages.Shared.AlertModel
@if (Model.Show)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @Model.Text
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
